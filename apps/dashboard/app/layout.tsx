import './styles.css';
export const metadata={title:'Sales-Connect Control Plane',description:'CRM sync operations dashboard'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
